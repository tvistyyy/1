#include <bits/stdc++.h>
using namespace std;

int main() 
{
     int a, b, c;
    cout << "Введите a,b,c: " << endl;
    cin >> a >> b >> c;
    
(a == b || b == c || c == a) ? cout << "Треугольник равнобедренный" : cout <<"Треугольник неравнобедренный";
    
    return 0;
}
